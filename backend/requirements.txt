# CLIMORA Backend — Python Dependencies
# ========================================
# Python 3.13.x  |  venv: backend/.venv/
#
# Install:
#   pip install -r requirements.txt

# -- Web framework (FastAPI replaces Flask) ---------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# -- Data validation --------------------------------------------------------
pydantic>=2.6.0

# -- Data processing --------------------------------------------------------
pandas==3.0.5
numpy==2.5.2

# -- ML stack (pinned — must match the serialized .pkl artifact) ------------
scikit-learn==1.9.0
xgboost==3.4.1
joblib==1.5.3

# -- Utilities --------------------------------------------------------------
requests==2.32.5
httpx>=0.27.0       # Used by environmental data services (weather, terrain, geocoding)

# -- Flask (archived — kept for rollback reference only) --------------------
# Flask==3.1.3
# Flask-Cors==6.0.5
